---
# Een upgrade-rol hoort juist naar latest te gaan; dat is het doel.
- name: Upgrade dnf packages  # noqa: package-latest
  ansible.builtin.dnf:
    name: "*"
    state: latest
    update_cache: true
  register: update_result
  notify:
    - Print updated packages
